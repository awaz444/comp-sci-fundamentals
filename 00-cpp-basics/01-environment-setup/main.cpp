#include <iostream>

// A minimal program to verify your toolchain is set up correctly.
// Compile: g++ -std=c++17 -Wall -o main main.cpp
// Run:     ./main
int main() {
    std::cout << "Environment is set up correctly!" << std::endl;
    return 0;
}
